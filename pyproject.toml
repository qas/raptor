[project]
name = "raptor-agent"
version = "0.1.0"
description = "A persistent, tool-using agent runtime"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.28,<1",
]

[dependency-groups]
release = [
    "pyinstaller>=6,<7",
]

[tool.uv]
package = false
